# No LFS needed — build artifacts are distributed via GitHub Release
# Only use LFS for non-rebuildable files > 50MB (currently none)

# Keep line endings consistent
*.swift text eol=lf
*.md text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
